import React from 'react';
import ReactDOM from 'react-dom/client'; // Thay đổi cách import
import App from './App';

// Tạo root và render ứng dụng
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);